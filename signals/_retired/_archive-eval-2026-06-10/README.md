# Eval / repro candidate archive — 2026-06-10

157 signal candidate `.md` files moved here from `signals/_inbox/_ai-candidates/`
because they were generated by eval-test runs (Phase 2.2–2.5 sweeps) and the
Phase 2.6 distillation-zero-delivery repro — not by real production briefs.

## How they got here

The eval harness (`tests/eval/run_eval.py` and friends) shares `VAULT_PATH` with
the production pipeline, so `_persist_external_drafts` writes candidate `.md`
files to the Obsidian inbox even for synthetic test briefs. Architectural quirk:
until eval and production vaults are separated, every Phase 2.x sweep pollutes
the inbox.

## Source runs

Signal ids cross-referenced against `03c_external_signals.json` in:

- `signal-compass-pipeline/tests/eval/results-*/eval-*/`
- `signal-compass-pipeline/runs/eval-*/`
- `signal-compass-pipeline/runs/repro-*/`

13 result folders scanned, 157 unique ids identified.

## Restoring a file

```bash
mv <id>.md ../../signals/_inbox/_ai-candidates/
```

## Generated at

2026-06-10
